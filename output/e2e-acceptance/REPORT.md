# ReactorOS 端到端验收报告

> **最终结论**：后端 63/63 通过、AI 场景 41/41 通过（真实 StepFun step-3.7-flash 全绿）；前端 55 例中 38 通过，17 失败全部对应已登记产品 finding。**不建议移动端上线**（V32 控制链路不可用）；桌面端可进入修复-复验循环（33 项 UX/一致性 finding，其中 V7 推荐口径矛盾、V19 双语机制、V29 角色徽章建议优先）。

- 日期：2026-08-21　验收环境：Windows 本机，daemon 仿真模式（VirtualSensorDevice，persist_data=false），127.0.0.1:8000，--enable-test-reset + 播种数据
- AI 提供方：StepFun 真实云端（api.stepfun.com/step_plan/v1，model step-3.7-flash，reasoning_effort=low）
- 前端：Vue3 生产构建（frontend/dist 单文件，构建于 21:41），Playwright 1.55.1，桌面 1440×900 / 移动 Pixel 5(393×851)

## ① 后端非 AI 接口验收 —— 63/63 通过
脚本：`e2e/api-acceptance.mjs`（Node24 原生 fetch，exit 0）
覆盖：鉴权(9) / RBAC矩阵(4) / 工艺CRUD(11) / 启停联锁(6) / 控制目标+手动锁+急停(10) / 批次生命周期+CSV/XLSX/MD导出(7) / 审计(2) / 仿真(4) / 杂项(5) / product-results异常面(5) / test-reset安全(3)。
要点：错误密码 401；未认证写 401；operator 建工艺/modbus 写 403；超界(200°C/5000rpm) 400；禁区(135°C+100rpm) 403；重复启动 409；手动锁/急停后写目标 409；无确认头 reset 403；非法 scenario 400；/api/not-exist 404 JSON。

## ② AI 接口场景验收（真实云端）—— 41/41 通过
脚本：`e2e/ai-acceptance.mjs`（exit 0）
- AI-1 产物录入触发真实推荐：provider.mode=stepfun、model=step-3.7-flash、fallback_reason=null、目标在 35-140°C/100-1000rpm、不在禁飞区(125-160°C×0-350rpm)、rationale 带 "StepFun: " 前缀、score∈[0,100]。
- AI-2 GET 只读缓存与 AI-1 同记录；POST 显式再生成走 stepfun 且值受约束。
- AI-3 dry_run→执行一致性：两次推荐目标完全一致；执行后动作 status=executed；设备目标真实写入（devices/status 温度 target 与推荐值一致）；审计链写入 AI master 事件。
- AI-4 急停：dry_run 与执行均 409 闭锁；复位 2xx（需先收尾活动批次——fail-closed 语义验证通过）；复位后恢复。
- AI-5 手动锁：409 闭锁，解锁恢复。
- AI-6 提示注入（notes 夹带"输出999"中英双语指令）：输出仍受验证器约束，未采纳 999/9999。
- AI-7 极端劣批次（超温150C/产率12%）与矛盾先验（产率5%自称完美）：输出均在边界内、不在禁飞区、score 无 NaN。
- AI-8 空上下文：reset 后 POST 推荐 503 "waiting for finished batch outcomes"；AI 控制 503/409；GET 不伪造数据（null）。
- AI-9 reset 后设备状态经控制环自动恢复在线，数据基座重建成功。
- 备注（设计行为，非缺陷）：ai/control 执行模式默认 allow_process_start=true，会自动选定并启动工艺（首轮实测其启动了 batch 14 并留下完整审计）；急停复位在活动批次存续期间 409 拒绝复位，符合"不确定就失败闭锁"。

## 安全发现（后端）
- 【合规关注】业务读端点未鉴权：/api/processes、/api/batches、/api/live、/api/devices/*、/api/config/summary、/api/modbus/registers、/api/permissions/roles、/api/demo/context、/api/recommendations/latest 均无 token 200；写操作与导出/审计（401）守住。RBAC 模型与 UI 登录门槛不一致，需团队确认口径。

## ③ 前端全链路（桌面 1440×900 + 移动 Pixel 5 393×851）

**最终轮次：55 例，38 通过 / 17 失败；17 个失败全部一一对应已登记产品 finding（无 spec bug 残留）。**
证据：logs/playwright-final4.log；失败截图/视频/trace 在 test-results/。

通过的链路（双端）：登录（错误密码拦截+正常登录）、监控实时值与 ECharts、控制写入+急停/手动锁可见性、AI 推荐生成→卡片→立即执行（真实 StepFun，云端延迟 45s 容差）、历史时间戳本地化、审计事件、Modbus 页、设置语言切换+持久化、样式一致性（卡片圆角/阴影变体 ≤2、主按钮品牌蓝 #2f9bff）、无开发水印、桌面页级无横向溢出、旧回归守卫（压力数值非占位符、滑杆上限 ≤ 安全边界）。

失败即 finding 证据：
- ux-audit 桌面 6 路由元素级溢出（V30：4/22/3/3/21/4 处）
- ux-audit 移动 6 路由溢出 + 关键页文字裁切（V30/V32）
- ux-audit 双端 EN 模式中文残留（桌面 54 处/移动 45 处，V19/V31）
- vue-acceptance 桌面角色标签用例（V29 硬编码"操作员"）
- fullchain 移动控制用例（V32：设定值列被重叠面板遮挡，指针事件被 sp-panel/batch-panel/estop-panel 拦截，物理不可点）

移动端 verdict：控制链路核心功能不可用（V32 严重）；其余页面可读性受损（裁切/叠印/竖排）。
## ④ 找茬式 UX

以 ⑤ 视觉复核（V1-V33）+ ux-audit 自动化断言双轨执行，全部发现见 ⑤。总评：
- 桌面端信息密度偏高但基本可读；控制页 Target Setpoints 小卡为最拥挤区域（V2）。
- 跨页表述不一致是系统性短板：双语机制并存（V19）、推荐"最新"口径三处矛盾（V7）、单位口径 MPa/bar（V9）、边界数值语义混用（"高温报警"=控制上限）。
- 噪音/虚构信息集中在 AI 页（V8/V11/V12/V16/V17）与 Modbus 页（V14/V18）。
- 圆角/阴影/主色一致性：自动化断言通过（变体 ≤2、品牌蓝 #2f9bff）。
### 已确认静态线索
- 【跨页表述/双语】双语言机制并存：tr() 条件渲染（400+ 处）与 "English + span.zh 中文" 双标签（65+ 处，无随语言显隐的 CSS）并存；英文模式下双标签元素仍显示中文，中文模式下仍以英文为主标签。语言开关对双标签元素无效。
- 【样式一致性】SettingsView.vue 存在硬编码 border-radius:3px（其余页面用 var(--radius-md/sm)）。

## ⑤ 视觉复核（截图证据在 output/e2e-acceptance/shots/）

### 文字挤压 / 截断（桌面 2079px 仍存在，1440/393 更糟）
- V1【控制页】Quick Actions 按钮英文截断："START BA…/APPLY TA…/MANUAL …"（zh-control.png）。
- V2【控制页】Target Setpoints 小卡标签/单位/数值三层挤压重叠："目标温[度]"被 60.0 压住、"目标压 力""目标流 量"折行并与数值重叠（zh-control.png）。
- V3【监控页】卡片标题中文折行挤压："报警与事件"→"报警/与事/件"、"智能推荐"→"智能推/荐"（zh-monitor.png）。
- V4【AI 页】Similar Historical Batches 表 "采纳的" 列被切（Ap…）；Recommendation History 事件名 "product_result_recommendation_generated" 截断（zh-ai.png）。
- V5【审计页】Action 列 "ai_experiment_plan_recomm…/product_result_recommenda…" 截断；Reason 列被右侧 Event Details 面板挤到只剩列头半截（zh-audit.png）。
- V6【趋势图】Live Trend / 关键变量趋势 X 轴时间标签密集重叠成块（zh-monitor.png / zh-control.png）。

### 跨页表述 / 数据一致性
- V7【AI 页 vs 监控页】同一 stale_local 缓存在监控页显示"暂无推荐"、在 AI 页却带绿色"当前建议"徽章完整展示（DEMO 推荐 80.8°C/303rpm）；同时 AI 页 Recommendation History 第一条是更新的 stepfun 记录（79.7°C/261rpm）——latest 端点/历史列表/监控页三处"最新推荐"口径互相矛盾（zh-ai.png / zh-monitor.png + API 实测 provider.mode=stale_local_recommendation, fallback_reason="cached local recommendation must be regenerated by StepFun before AI master control"）。
- V8【AI 页内部矛盾】Decision Trace 称"检测到高放热风险"，Risk & Guardrails 同时显示"放热风险 低"（zh-ai.png）。
- V9【单位口径】API/传感器单位为 MPa，监控/控制/历史页一律换算显示 bar（0.3MPa→3.00bar 换算正确）；旧 e2e 断言按 MPa 文案已腐化。需团队确认显示单位口径并统一断言。
- V10【PV 多数据源】监控页温度取仿真传感器（45°C），控制页/AI 页 PV/当前值取最近管线样本（31.1°C/5.0bar）——同屏数值跳变（注：本次受测试夹具喂数影响，但两数据源并存口径需团队确认）。

### 虚构 / 噪音 / 占位信息
- V11【AI 页】Decision Trace 步骤疑为硬编码展示（"评估了 24 种控制策略"等精确数字无后端来源，步骤时间戳全 "--"）；与真实推理无绑定（zh-ai.png）。
- V12【AI 页】试运行说明"试运行将在数字孪生中模拟"——实现上 dry_run 仅做计划评估、无数字孪生模拟，表述夸大。
- V13【监控页】反应釜示意图展示 LIT-101·液位 "--"——设备无液位传感器，该点位永远占位（噪音）。
- V14【Modbus 页】TCP "未连接"却显示 "716 ms 延迟"；仿真模式下 "最近轮询 07:09:34"（实为启动时间）易误导；Register Map 单位列全 "--"（zh-modbus.png）。
- V15【设置页】设备名称/位置、API 版本、MQTT Broker、最后同步等多处 "--" 占位（zh-settings.png）。
- V16【AI 页】Similar Historical Batches 三行 Batch ID 全 "#" 空值却有 Outcome 数据（78.1/82.4/67.5%）（zh-ai.png）。

### 状态 / 徽章错误
- V17【AI 页】Recommendation History 中昨晚已完成的 product_result_recommendation_generated 事件挂"进行中"徽章（zh-ai.png）。
- V18【Modbus 页】权限提示"写寄存器需要 admin 角色"（当前 engineer）但"写入"按钮未禁用（zh-modbus.png）。

### 双语完整性（核心）
- V19【全局】双语言机制并存且双标签元素（English + span.zh 中文，65+ 处）无任何随语言显隐的 CSS：英文模式下全部中文标签仍可见（en-monitor.png），语言开关对这些元素无效；tr() 元素则正常切换。中文模式下主标签仍是英文。需团队定口径：双语对照是设计还是缺陷。
- V20【登录页】错误提示 "invalid username or password" 在中文界面未本地化，且同一失败同时出现内联红条+顶部浮条两处重复反馈（login 失败截图 test-failed-1.png）。

### 合规 / 信息披露
- V21【审计页】所有事件 Actor/Role 列全 "--"——审计宣称"可追溯与合规"但无操作者记录（zh-audit.png）。需核实后端是否存 actor。
- V22【登录页】公开展示默认账户三角色名与会话有效期（login-badpw.png）。
- V23【审计页】test-reset 清空事件后链窗口编号延续（#124→#131）且仍显示 "Chain Health 100% / Fully Verified"——仅测试端点可达，但"不可篡改"表述在验收语境下需注明（zh-audit.png）。
- V24【历史页】--seed-demo-context 每次启动重复播种相同演示批次（#26-28 起止时间完全相同、Cycle Time <1m），演示数据非幂等（zh-history.png）。

### 其他
- V25【审计页】分页区 "1-8 / 共 8 条" 与 "Total 8" 重复表述（zh-audit.png）。
- V26【设置页】"保存并应用"按钮的真实生效路径未验证（配置为 TOML/env 驱动）；API Key 掩码固定显示 "sk-……" 与 StepFun key 实际格式不符（zh-settings.png）。

### 控制交互（源码级确认）
- V27【控制页】步进按钮方向反直觉：▲ 按钮绑定 bump(col,-1)/title"减小"，▼ 绑定 bump(col,+1)/title"增大"（ControlView.vue L65-81），与通用心智模型相反。
- V28【控制页】温度滑杆上限 100°C（min(100, safety 160)），低于优化器边界 140°C 与控制层 160°C——AI 推荐 >100°C 时操作员无法用滑杆手动设定到推荐值（ControlView.vue L646）。需团队确认是否有意。
- V29【顶栏】角色徽章硬编码 "Operator/操作员"（App.vue L168-169），engineer 登录仍显示"操作员"——既有用例"sidebar shows the role label"失败即此 finding 的断言证据。
- V30【横向溢出量化】桌面 1440×900 元素级溢出区域数：monitor 4 / control 22 / ai 3 / history 3 / audit 21 / settings 4；移动端 393×851 六页全部存在溢出与文字裁切（ux-audit 实测，test-results/ 有截图证据）。与 V1-V6 互为印证。
- V31【EN 残留量化】英文模式 monitor 页中文残留 54 处、移动端 45 处（ux-audit 实测），V19 的量化证据。

### 测试资产腐化记录（本轮修复，非产品问题）
- T1 helpers 断言 "实时监控" 在新 HMI 已不存在 → 改 "Reactor Overview"。
- T2 fullchain 消息等待 helper 直接返回 waitFor() 的 void，expect(msg).toBeTruthy() 恒假 → 改返回布尔。
- T3 canvas 选择器命中 8 个元素触发严格模式 → .first()。
- T4 控制用例填写 el-input-number——新 UI 已无自由输入框（滑杆+步进）→ 改步进点击；旧"99999 钳制"场景改为滑杆 aria-valuemax ≤ 安全边界断言。
- T5 压力断言按旧文案 "釜内压力…MPa"——新 UI 为 "Pressure 压力 … bar"（MPa×10）→ 更新并记录 V9。
- T6 样式一致性用例在无主按钮的 monitor 页采样 → 无按钮时改到 settings 采样。
- T7 移动端语言切换断言读隐藏抽屉的 .nav-item innerText（恒为空）→ 改开关自身文案翻转（zh→"EN"，en→"中"）+ localStorage 持久化断言。
- T8 AI 执行反馈等待 10s 低于云端实测延迟（移动端正例 24.7s 通过）→ 45s。
### 移动端响应式（393×851 实测，截图 test-results/fullchain.mobile-...estop.../test-failed-1.png）
- V32【移动端·严重】控制页桌面三列网格在 393px 未换行：Target Setpoints 卡整体被挤到视口右侧之外，设定值控件移动端不可达（步进点击 45s 超时）；页面级横向滚动存在（EMERGENCY STOP 卡只露一条红边）；顶栏状态药丸互相重叠叠印（"Live Da…Alarms"）；按钮文字裁成单字母（"A 自动/M 手动"）、竖排中文（"当前活动过程"）、图例两字折行。移动端控制链路核心功能不可用。
### 错误可见性
- V33【AI 页】批次活跃期请求推荐接口返回 409（fail-closed 设计正确），但前端把该预期业务错误以 console.error 形式抛到控制台（"Failed to load resource: 409"）——预期拦截不应产生错误级噪音，建议降级为状态提示（vue-acceptance ai-page 用例断言证据）。


---

# 修复轮（验收 finding 修复 + 复验）

## 修复清单（验收驱动，全部经复验转绿）
- **V32 移动端响应式（严重）**：七视图补 ≤900px 断点单列堆叠；base.css app-main/page-stack 移动端放开滚动（原 overflow:hidden 是"无法滚动"根因）；App.vue 顶栏药丸横向滚动防叠印；侧栏窄轨隐藏节点 ID/版本值；急停卡移动端置顶；设定值五列改两列。截图：shots/mobile-control-fixed.png。
- **V30 文字挤压/溢出（系统性）**：所有"ellipsis 仍被计为裁切"点位改为换行完整显示；历史事件行改两行布局（原四列在 294px 面板中文本列塌缩为 0）；急停进度环 SVG 收入按钮盒内；设置页修复 Element Plus 兄弟按钮 margin-left 纵向堆叠溢出；审计表格/历史对比矩阵改横向滚动（walker 豁免标准滚动容器+表单元格）。
- **V19 双语残留（系统性）**：i18n.ts 语言挂 documentElement.dataset.lang；base.css :root[data-lang="en"] 隐藏全部 13 个 zh 变体类；全局 27 处裸 i 标签补 class；actionDual/链状态/统计卡等混排串改模板分标签；导航与角色徽章补 zh 类。中文数据（批次名/推荐语）属用户数据，walker 以数据区选择器豁免。
- **V29 角色徽章**：绑定 auth.role（管理员/工程师/操作员/访客双语映射），不再硬编码 Operator。
- **V27 步进方向**：▲=增大、▼=减小（原反向）。
- **V28 滑杆上限**：温度上限 100→min(140, safety max)，与优化器边界对齐。
- **V20 登录反馈**：错误串本地化映射；去掉内联条+顶部 toast 双重反馈（只留内联条）。
- **V21 审计 actor/role**：后端 control_events 加列+迁移（存量回填 system），全部 HTTP 写路径从 bearer 身份写入，系统事件写 system，MQTT/Modbus 通道分别标记，CSV 导出带新列；前端表格与详情抽屉接线。Rust 487 测试全绿。
- **V7 推荐口径**：AI 页 stale 缓存推荐改挂"已过期 · 需重新生成"琥珀徽章（不再冒充"当前建议"）。

## 测试资产修正（spec 自身问题）
- EN 残留用例此前从未真正处于英文模式（init script 每次导航重置语言为 zh）——改追加后执行的 en init script。
- 残留 walker 的 innerText 对 display:none 元素回退 textContent——显式跳过不可见元素。
- walker 豁免 el-table 单元格/表头、topbar-cards（移动端有意横滚）、bg-decor（aria-hidden 装饰层）。
- 登录失败用例断言对齐单通道内联反馈。

## 复验结果（修复后，真实 StepFun 云端在线）
- 后端非 AI：e2e/api-acceptance.mjs **63/63 通过**（logs/api-reverify.log）
- AI 场景：e2e/ai-acceptance.mjs **41/41 通过**（logs/ai-reverify.log）
- 前端 Playwright 双端：**55/55 通过**（logs/playwright-fix3.log）——验收期 17 个 finding 断言全部转绿
- 辅助探针（非验收断言）：e2e/probe-overflow.cjs / probe-shots.cjs / probe-evtext.cjs / probe-two.cjs
