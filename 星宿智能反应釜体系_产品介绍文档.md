# 星宿智能反应釜体系 (Xingshu Intelligent Reactor System)
## 边缘控制与 AI 决策节点 —— 产品介绍文档 (PRD)

---

## 一、 产品定位与愿景

**星宿智能反应釜体系**是面向现代化学、制药、材料及生物精细化工研发与中试的**全栈式智能反应釜解决方案**。

本系统定位于**“安全为底座，AI为大脑，边缘为载体，审计为纽带”**的闭环控制层。它不单是一个上位机软件，而是一整套向下兼容多种硬件执行器、向上对接先进AI大模型、本地进行高频安全限幅与多维数据落盘的现代化工业智能系统。

它的愿景是**打破昂贵笨重工业控制系统的门槛，让每一个实验室和中试车间都能以极低的成本拥有具备“自我进化能力、绝对安全保障”的智能反应釜系统**。

---

## 二、 行业传统痛点

在精细化工与制药研发中，传统的反应釜控制方案通常面临以下困境：

1. **热惯性失控与“飞温”风险**：化学放热反应具有强烈的非线性与时滞性。传统 PID 控制器面对复杂的反应物料时，常常因为调参不当造成温度严重过冲（飞温），轻则导致实验失败、物料报废，重则引发物理爆炸。
2. **“黑箱”经验主义，配方难以沉淀**：不同批次的反应产率和物料比例差异很大。传统系统只能被动执行死板的配方，无法从历史失败或成功批次中自我学习，工艺优化完全依赖人工经验，缺乏数据支撑。
3. **系统臃肿高昂，部署门槛高**：大型分布式控制系统（DCS）价格动辄数十万元，且对厂房布线、抗干扰要求极高，极难在科研实验室或快速迭代的中试线上铺开。而简单的 PLC 方案又缺乏高级计算能力与便捷的信息化交互。
4. **数据易丢失，审计难以追溯**：传统的试验数据多为人工抄录，或者存在易被篡改的上位机Excel表格中，缺乏对于人工干预（如改温、手动调速）、异常警报与传感器读数的“秒级本地不可篡改审计追踪”，无法满足合规与工艺改进的可信分析需求。

---

## 三、 星宿体系五大核心产品特性

星宿智能反应釜体系通过软硬件解耦的全新架构，为上述痛点提供了创新的解法：

```
                    +---------------------------------------+
                    |        云端/本地大模型决策中心       |
                    |         (StepFun AI step-3.6)         |
                    +---------------------------------------+
                                        |  (推荐参数)
                                        v
                    +---------------------------------------+
                    |          星宿本地安全过滤器           |
                    |         (温度/转速/步长限制)          |
                    +---------------------------------------+
                                        |  (安全指令)
                                        v
+------------------+  串口 / 文件  +---------------------------------------+  本地记录  +------------------+
|  底层反应釜硬件  | <===========> |     星宿边缘中枢 Rust 守护进程        | ---------> |  SQLite 本地审计 |
| (ESP32/驱动/PLC) |               |       (搭载于树莓派/鲁班猫开发板)     |            |  (秒级高频落盘)  |
+------------------+               +---------------------------------------+            +------------------+
                                                |  (静态Web/Qt托管)
                                                v
                                   +-------------------------+
                                   |      双端交互控制台     |
                                   |    (PC Web / 移动端 UI) |
                                   +-------------------------+
```

### 1. 独创的“双擎驱动”AI 工艺大脑
星宿体系内置了本地轻量级算法与云端 **StepFun AI (`step-3.6`) 大模型** 双引擎决策机制。
* **自学习自进化**：大模型根据 SQLite 中积累的历史反应批次（包括人工录入的产率、产物比例、温度曲线、反应时间）进行多维分析，推理出能获得最大产率的最优温度及搅拌转速。
* **安全沙盒拦截**：AI 产生的一切控制指令，在下发到底层硬件前，必须经过本地物理安全过滤器（[safety.toml](file:///x:/tianhks/config/safety.toml)）的校验。凡是落入人工设定的禁区（`forbidden_zones`）或超出物理范围的参数，一律被本地强制拦截并告警，杜绝了大模型的“幻觉飞温”。

### 2. 独立的“安全卫士”控制锁 (Safety Guard)
安全是反应釜体系的生命线。星宿体系在软件边缘层构建了独立的安全校验模块：
* **单次步长限制**：限制单次温控/转速调整的最大变化幅度（例如每次调节温差不超过 5℃），依靠渐进式逼近，有效平抑物料温控的时滞与热惯性。
* **全方位联动保护**：当发生**急停触发**、**人工主动锁定**、或**数据链路超时（防止传感器掉线导致温控盲目加热）**时，系统立即自动切断自动控制，进入安全保护状态，下发停止指令。

### 3. 多维度本地可信数据审计
* **秒级本地持久化**：系统采用高度优化的本地 SQLite 数据库，对传感器状态、报警队列、AI 决策记录进行秒级自动持久化。
* **不可篡改审计流**：记录每一次的人工指令更改（如“何时何人更改目标温度为 120℃”）、自动控制状态切换以及急停重置事件。即使在无网或离线环境中，依然能够为研发流程提供符合 FDA/工业级标准的审计追踪日志。

### 4. 松耦合、多模态的桥接 (Bridge) 架构
星宿体系并不强绑定某一特定硬件品牌的反应釜。它通过底层桥接层提供极佳的兼容性：
* **ESP32 串口桥接**：提供配套的开源 C++ 桥接固件（[esp32_reactor_bridge](file:///x:/tianhks/firmware/esp32_reactor_bridge)），直接通过 UART/RS485 与硬件交互。
* **JSON 文件读写桥接**：采用读写分离的 `state.json` 与 `control.json` 原子级文件桥接模式，轻松兼容已有的各类上位机采集系统。
* **Modbus/外部数据管线**：支持通过数据流直接进行高频数据注入与控制下发。

### 5. 极致轻量化的边缘部署
* **单进程无依赖**：核心守护进程由 **Rust** 开发，无额外垃圾回收（GC）开销，启动极快，内存占用通常低于 30MB。
* **一键离线部署**：不依赖庞大的微服务集群，单个二进制文件搭载原生 HTML/CSS/JS 静态 Web 交互台，在无网络的深山工厂或密闭实验室内同样能以最完美的状态离线运行。

---

## 四、 系统技术规格与指标

| 模块 | 指标项 | 规格与表现 |
| --- | --- | --- |
| **基础运行** | 推荐硬件平台 | 树莓派 4B/5，鲁班猫 2/3 (Cortex-A55 ARM64) 或更高配置 |
| | 软件核心 | Rust 1.75+ / Tokio 异步运行时 / Axum Web 服务 |
| | 内存/CPU 占用 | 内存 < 50MB，单核 CPU 占用通常 < 3% |
| **数据采集** | 采样频率 | 硬件端默认 1Hz ~ 10Hz (秒级高频采样) |
| | 监测指标数 | 7大核心指标：温度、压力、转速、摇速、流量、浓度、pH |
| **安全控制** | 响应延迟 | 传感器掉线/数据超时检测阈值默认为 6,000ms |
| | 限制参数 | 目标温度上限、目标转速上限、温度调整步长、转速调整步长 |
| **智能决策** | 决策机制 | StepFun AI (`step-3.6`) 聊天/信息 API + 本地遗传/退火算法 |
| | 安全围栏 | 人工禁区校验（温度-转速双维区间防跨界拦截） |
| **系统审计** | 存储介质 | SQLite 本地嵌入式数据库文件 (`data/reactor.sqlite3`) |
| | 审计数据流 | `control_events`（控制流）, `process_audit_logs`（生命周期与异常） |

---

## 五、 应用场景

1. **高校与科研院所配方探索**：
   通过星宿体系的“AI 优化推荐”与历史批次对比功能，科研人员可以大幅缩短摸索新型催化反应最优温控与搅拌区间的实验周期。
2. **制药与精细化工中试放大**：
   在实验室小试向工厂大生产放大的中试阶段，星宿体系的“单次步长限幅”与“多重报警联动”能极大保护贵重的中试反应原料，防止发生重大飞温事故。
3. **老旧反应釜设备智能化低成本改造**：
   对于大量已经配备了基础 PLC 或模拟仪表的存量反应釜，只需挂载一个低成本的 ESP32 串口模块和树莓派，即可升级为拥有现代 Web 控制台 and AI 参数优化的大模型智能反应釜。

---

## 六、 交付物清单

1. **星宿中枢守护程序 (Edge Daemon)**：Rust 编译的高性能单二进制文件。
2. **极简交互控制台 (Web Dashboard)**：嵌入守护程序的原生单页 Web 上位机控制台。
3. **物理适配固件 (Bridge Firmware)**：适用于 ESP32 的配套串口/RS485 通信桥接固件。
4. **安全与 AI 记忆配置文件组**：
   * [safety.toml](file:///x:/tianhks/config/safety.toml) (安全限幅配置文件)
   * [ai_memory.toml](file:///x:/tianhks/config/ai_memory.toml) (AI 参考批次与禁区边界配置文件)
   * [device.toml](file:///x:/tianhks/config/device.toml) (物理设备适配配置文件)
